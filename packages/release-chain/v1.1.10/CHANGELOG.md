# v1.1.10

Build date: 2026-04-25

## 更新内容

- 计划编辑页支持点击参数数值后手动输入，覆盖组数、每组目标、每组时长、每组递减、负重和间歇。
- 保留原来的 `+/-` 微调方式，并同步提高参数上限，避免大跨度参数被旧上限截回。
- 新增输入范围校验，整数参数不接受小数，负重支持小数输入。

## 验证

- `assembleDebug` 通过。
- `testDebugUnitTest` 通过。

## APK

- `app-debug-20260425-v1.1.10.apk`
